# Chrome Extension Setup

See the repository file `docs/CHROME_EXTENSION_SETUP.md` for the full local
Chrome extension setup guide. This deployed copy exists so the demo site's docs
links have a stable static target.
